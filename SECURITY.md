# 安全说明

## 支持范围

当前项目面向校内教学组抢选场景。生产部署应仅开放给受控校园网或可信反向代理，并启用 HTTPS。

## 报告问题

请勿在公开 Issue 中粘贴学生名单、学号、激活码、管理员凭据、日志原文或数据库文件。发现安全问题时，请通过仓库维护者的 GitHub 私密联系方式报告。

## 部署底线

- 使用随机且唯一的 `APP_SECRET` 与管理员初始密码。
- 不提交 `.env`、数据库、备份和私钥。
- Compose 源站端口保持 `ORIGIN_BIND=127.0.0.1`，只允许同机 Cloudflare Tunnel 访问；若明确启用局域网直连，必须同时配置主机防火墙和可信来源限制。
- 生产发布只接受经审核的完整 40 位 Git 提交号，不使用可移动的分支、标签或缩写提交号。
- 持续启用 HTTPS 后分阶段设置非零 HSTS `max-age`；未审计所有子域前不启用 `includeSubDomains` 或 `preload`。
- 学生端只返回完成当前操作所需的最少数据。
- 激活码明文只允许通过管理员当前活动接口按需解密；响应禁止缓存、操作受限流和审计保护，前端默认掩码并自动隐藏。
- 候场心跳只表示最近在线状态，不作为身份、开抢权限或名额判定依据；倒计时结束时间由服务端事务校验。
- CI 必须通过生产与开发 requirements 的 `pip-audit` 门禁；基础镜像 digest 只能使用在目标架构上核验过的值。
- 本机备份只用于快速回滚；生产数据必须在离开 VM 前加密，并复制到独立故障域。远端凭据与加密密钥不得进入仓库或项目 `.env`，且必须定期执行隔离恢复演练。
- 上线前完成并发末位名额测试、备份恢复测试、HSTS 验收和权限检查。
