# 示例数据

`fictional-students-180.csv` 是自动化验收专用的虚构名单，不含任何真实学生信息：

- 共 180 人，学号统一以 `TEST2026` 开头；
- 建筑学、城乡规划、风景园林各 60 人；
- 只提供“学号、姓名、专业”三列，激活码由系统导入时随机生成；
- 可用于关闭状态下的测试活动，不得导入正在使用的正式活动。

配套回归见 `tests/test_fictional_roster_flow.py`。测试会验证批量导入、180 个唯一激活码、
同一出口 IP 并发登录、原子抢位、结果导出、活动归档及旧会话失效。
